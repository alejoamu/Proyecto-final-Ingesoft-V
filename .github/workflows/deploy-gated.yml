name: Deploy (Gated)

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Ambiente a desplegar'
        required: true
        default: 'stage'
        type: choice
        options: [stage, production]
      version:
        description: 'Tag/version a desplegar (semver)'
        required: true

jobs:
  deploy:
    name: Deploy con aprobaciones
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Mostrar parámetros
        run: |
          echo "Environment: ${{ github.event.inputs.environment }}"
          echo "Version: ${{ github.event.inputs.version }}"

      - name: Despliegue (placeholder)
        run: |
          echo "Desplegando version ${{ github.event.inputs.version }} a ${{ github.event.inputs.environment }}"
          # kubectl apply -k k8s -n ${{ github.event.inputs.environment }}

      - name: Health check (placeholder)
        run: |
          echo "Verificando salud de despliegue..."

